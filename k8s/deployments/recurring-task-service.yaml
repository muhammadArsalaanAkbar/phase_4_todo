# Task: T072 — Recurring Task Service K8s deployment manifest
# Deployment + Service + Dapr annotations
# Port 8005, app-id=recurring-task-service, non-root UID 1001
apiVersion: apps/v1
kind: Deployment
metadata:
  name: recurring-task-service
  namespace: todo-dev
  labels:
    app: recurring-task-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: recurring-task-service
  template:
    metadata:
      labels:
        app: recurring-task-service
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "recurring-task-service"
        dapr.io/app-port: "8005"
        dapr.io/app-protocol: "http"
        dapr.io/enable-api-logging: "true"
    spec:
      containers:
        - name: recurring-task-service
          image: recurring-task-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8005
              name: http
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: dapr-db-secret
                  key: connectionString
            - name: LOG_LEVEL
              value: "INFO"
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8005
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8005
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 3
          startupProbe:
            httpGet:
              path: /health
              port: 8005
            initialDelaySeconds: 5
            periodSeconds: 3
            failureThreshold: 20
      securityContext:
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
---
apiVersion: v1
kind: Service
metadata:
  name: recurring-task-service
  namespace: todo-dev
  labels:
    app: recurring-task-service
spec:
  type: ClusterIP
  selector:
    app: recurring-task-service
  ports:
    - port: 8005
      targetPort: 8005
      name: http
