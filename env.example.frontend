# Frontend Environment Variables
# Copy to .env.local and fill in values

# =============================================================================
# PUBLIC CONFIGURATION (safe to commit via ConfigMap)
# =============================================================================

# Environment mode
NODE_ENV=production

# Backend API endpoint (injected at runtime)
NEXT_PUBLIC_API_URL=http://todo-backend-svc:8000

# =============================================================================
# OPTIONAL SECRETS (if any frontend-specific secrets needed)
# =============================================================================

# Currently no frontend-specific secrets required
# All sensitive configuration is handled by the backend
